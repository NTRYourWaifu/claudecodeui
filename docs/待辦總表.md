# CloudCLI Fork 待辦總表

> 這份是**開工文件**，不是紀錄文件。拿了就能做。
> 對應專案：`F:\Vs\cloudcli`（siteboon/claudecodeui fork，AGPL-3.0）
> 建立：2026-08-18 ｜ 還原點 commit：`776fcd3`

---

## 0. 這個專案的鐵律（違反會壞掉，每次動工前看一眼）

| 禁止 | 為什麼 |
|---|---|
| **不准跑 `npm install` / `npm update`** | 會覆蓋掉 `node_modules/@anthropic-ai/claude-agent-sdk/sdk.mjs` 的 cross-drive cwd 修補。沖掉之後這台機器上**完全不能對話**（claude.exe spawn ENOENT）。真的必須跑，事後要重跑 `patch-sdk.cjs` / `patch-sdk2.cjs` |
| **不准 `git pull` / `git merge origin/main`** | 上游領先 161 個 commit（我們 v1.32.0 → 上游 v1.37.1）。要抄功能就手動抄單檔，不整批合併 |
| **`.bat` 內容一律純英文** | Windows cmd 用 Big5 解析 UTF-8 中文 → 亂碼被當指令跑 → exit 1，雙擊一閃就死 |
| **Windows 路徑 drive letter 一律大寫** | `process.cwd()` 在 Windows 一律回大寫，canonical path 必須跟著大寫，否則專案清單會分裂成兩筆 |
| **不動認證機制** | 自家帳密 + JWT，單使用者，能用就別碰。忘記密碼救援只有 `reset-password.bat` |
| **改完 Claude 自己跑 `npm run build`** | 使用者不寫程式碼、不手打 npm 指令。他的驗證方式是雙擊 `restart-prod.bat` |

**驗證方式（每項工作完成後）**：
1. Claude 自己跑 `npm run build`（client + server 都要）
2. 請使用者雙擊 `restart-prod.bat`，用手機開 `https://desktop-r37hqa9.tail58509a.ts.net` 看
3. 純視覺的項目（配色、圓角、密度、刻度條）**Claude 驗不了**，一律標 ❌ 未測，等使用者回報

---

## 1. 系統匣常駐圖示 ⭐ 最想要

**決策**：用 PowerShell 7 + WinForms `NotifyIcon` 做常駐托盤，**不做開機自啟**（使用者明確選擇維持手動雙擊）。

**已驗證**：`pwsh -NoProfile -Command "Add-Type -AssemblyName System.Windows.Forms; New-Object System.Windows.Forms.NotifyIcon"` 在這台實測成功回傳物件。零額外依賴，不需要 Electron、不需要 npm 套件。

**契約**：
- 新檔 `F:\Vs\cloudcli\tray.ps1` ＋ 啟動器 `F:\Vs\cloudcli\tray.bat`（純英文）
- 伺服器以**獨立於 log 視窗的行程**啟動 — 關 log 視窗不得中斷伺服器。這是整個需求的核心，不能用「cmd 當 parent」那種寫法
- 左鍵單擊 → 開瀏覽器到 `http://localhost:3001`
- 右鍵選單四項：`Open in Browser` / `Show Log` `Hide Log`（切換）/ `Restart Server` / `Exit`
- `Restart Server` 要先釋放 :3001 再起新的（沿用 `restart-prod.bat` 的 `netstat | findstr LISTENING` + `taskkill /F` 邏輯）
- `Exit` 要同時關掉伺服器行程，不能留孤兒佔著 :3001

**邊界條件**：
- 已經有伺服器在 :3001 → 不要重複啟動，托盤直接接管既有行程（或提示）
- 伺服器自己掛掉 → 托盤圖示要能反映（換 icon 或 tooltip 標示 stopped），不能顯示綠燈但其實死了
- 圖示來源：`F:\Vs\cloudcli\public\` 底下已有 8 種尺寸 PWA icon，挑一個轉 `.ico` 或直接用 `System.Drawing.Icon.ExtractAssociatedIcon`

⚠️ **風險**：完整互動（選單點擊、log 顯示隱藏、重啟後圖示狀態）**Claude 沒辦法自動驗**，只能請使用者實際點過一輪。托盤程式沒有測試框架。

---

## 2. 拔掉 Upgrade 按鈕 ⭐ 危險品優先處理

**決策**：前端按鈕 **和** 後端端點**都要拔**，不是只隱藏 UI。

**已驗證（這是真的會壞事，不是預防性擔憂）**：
`server/index.js:225` 的 `POST /api/system/update` 會執行：
```
git checkout main && git pull && npm install
```
三個各自獨立的災難：
1. `npm install` 沖掉 SDK 的 cross-drive 修補 → 這台**不能對話**
2. `git pull` 面對上游 161 個 commit 的落差 → 衝突或半殘狀態
3. `spawn('sh', ...)` 在 Windows 上多半找不到 `sh` → 但**不能賭它一定失敗**

**要動的絕對路徑**：
- `F:\Vs\cloudcli\server\index.js:225`（整個 `/api/system/update` 端點移除，或改成直接回 410 並寫一行 log 說明本 fork 停用自動更新）
- `F:\Vs\cloudcli\src\hooks\useVersionCheck.ts`（停掉對 GitHub releases 的輪詢，否則還是會跳紅點）
- `F:\Vs\cloudcli\src\components\sidebar\view\subcomponents\SidebarFooter.tsx`（升級入口按鈕）
- `F:\Vs\cloudcli\src\components\sidebar\view\subcomponents\SidebarModals.tsx:211`（`VersionUpgradeModal` 掛載點）
- `F:\Vs\cloudcli\src\components\settings\view\tabs\AboutTab.tsx` 與 `.../api-settings/sections/VersionInfoSection.tsx`（設定頁裡的版本／升級區塊）

**保留**：版本號**顯示**可以留（知道自己在 v1.32.0 有用），拔掉的是「檢查更新」和「執行更新」。

---

## 3. Enter 換行（只有手機）

**決策**：**只有手機**改成 Enter 換行，電腦維持 Enter 送出（使用者明確選這個，不是全域改）。

**已驗證**：機制本來就存在，不用新寫。
- 開關定義：`F:\Vs\cloudcli\src\hooks\useUiPreferences.ts:40` → `sendByCtrlEnter: false`
- 生效位置：`F:\Vs\cloudcli\src\components\chat\hooks\useChatComposerState.ts:836`
  現行邏輯：`!shift && !ctrl && !meta && !sendByCtrlEnter` → 送出

**契約**：判斷「是不是手機」要用 App 既有的 `isMobile` 判定，**不要新造一套** breakpoint。目前 `isMobile` 是從上層一路傳下來的 prop（`MainContent` 有拿到），需要讓它到得了 composer 的 keydown 邏輯。

**邊界條件**：
- 輸入法組字中（`isComposing`）已經有擋，**不要動那段**，動了中文輸入會壞
- 電腦上 `Ctrl+Enter` 送出必須繼續有效
- 手機上除了送出鍵，不該有任何按鍵組合會送出

---

## 4. context 分母跟著模型走

**決策**：分母改成查表，跟當下選的模型走。Opus / Sonnet = 1,000,000，Haiku 4.5 = 200,000。

**已驗證是 bug**：三處各自寫死、算法還不一致
| 位置 | 分母 | 「已用」怎麼算 |
|---|---|---|
| `server\claude-sdk.js:333` | 160000 | input + output + cacheRead + cacheCreation |
| `server\index.js:1269` | 160000 | input + cacheCreation + cacheRead（**不含 output**）|
| `src\...\ChatComposer.tsx:352` | 160000（前端 fallback）| — |

**契約**：
- 分母查表放 `F:\Vs\cloudcli\shared\modelConstants.js`，跟 `CLAUDE_MODELS.OPTIONS` 放在一起（該檔註解已經寫著各模型的 context 大小，把它變成程式讀得到的欄位而不是註解）
- **已用量的算法要統一成一種**，兩個端點不能各算各的。以 `server\index.js` 那版（不含 output）為準——這跟 ccusage 口徑一致，註解已寫明
- 非 Claude 供應商（Codex 200k 等）維持各自既有值，**這次不要一起改**

⚠️ **前提未驗證**：畫面顯示 1M **不等於**底層真的吃得到 1M。1M context 在 Claude 這邊可能需要額外開關。這項要**單獨驗過再宣稱完成**，不准用「應該可以」帶過。驗法：實際跑一個超過 20 萬 token 的對話看會不會被截斷，或查 SDK 有沒有傳對應的 beta 參數。

---

## 5. 工具調用壓成一行

**決策**：一律壓一行，**保留點一下展開全文**。

**已驗證元凶**：`F:\Vs\cloudcli\src\components\chat\tools\configs\toolConfigs.ts:49` 的 `Bash` 設定
```
type: 'one-line', getValue: (input) => input.command, wrapText: true
```
`wrapText: true` 讓完整指令全文換行攤開，長指令吃掉半個螢幕。

**契約**：
- 一行的內容格式：**做了什麼 ＋ 對哪個目標**（例：`執行指令 · npm run build`）。使用者原話：「你可能就寫個調用工具。調用什麼工具，然後什麼文件就好了」
- 超出寬度用省略號截斷，**絕不換行**
- 點一下展開看全文
- 這是**顯示層**的改動，`showRawParameters` / `autoExpandTools` 兩個既有偏好設定的語意不要改壞

**邊界條件**：
- `TaskList` / `TaskGet` / `AskUserQuestion` / `ExitPlanMode` 目前是 `defaultOpen: true`（`toolConfigs.ts:343,366,479,499,515`），**這幾個要維持展開** — 它們是要你讀的內容，不是雜訊
- 未知工具走 `Default` 設定（`toolConfigs.ts:530`），已經是 `defaultOpen: false`，維持

---

## 6. 主頁改成專案／對話清單

**決策**：一進來直接是清單，選了才進對話。

**要動的絕對路徑**：`F:\Vs\cloudcli\src\components\main-content\view\subcomponents\MainContentStateView.tsx` — 那張「請選擇專案」的空白卡片就是它。

**契約**：清單內容要**重用側欄既有的專案／對話元件**（`src\components\sidebar\`），不要另寫一份清單邏輯。側欄已經做好「按專案 accordion + 預設 5 條 + 觸底無限滾動」，重寫一份等於製造兩套會走鐘的東西。

**邊界條件**：
- 手機和桌機都要對（手機側欄是抽屜，桌機是常駐欄）
- 一個專案都沒有的全新狀態，要有辦法走到「建立專案」，不能變成死路

---

## 7. 輸入欄按鈕收納

**決策**：**照抄 VSCode 裡 Claude Code 擴充套件的排版** — 左邊兩顆（`+` 和 `/`），右邊一顆模式選擇（含 effort）。這是使用者的補充註記，比表單選項優先。

**現況**：輸入框下方有 7 顆（`F:\Vs\cloudcli\src\components\chat\view\subcomponents\ChatComposer.tsx:324-379`）
加圖片 ／ 權限模式 ／ 模型+effort+thinking ／ 帳號用量 ／ context 圓餅 ／ 指令選單 ／ 清空輸入

**收納後**：
| 位置 | 按鈕 | 收哪些進去 |
|---|---|---|
| 左 | `+` | 加圖片、清空輸入 |
| 左 | `/` | 指令選單（原本的 `MessageSquareIcon`） |
| 右 | 模式選擇 | 權限模式、模型、effort、thinking、帳號用量、context |

**參考實作**：`C:\Users\yee\.vscode\extensions\anthropic.claude-code-2.1.234-win32-x64\webview\index.css`（要對排版細節時看這份，別憑印象）

**邊界條件**：既有元件 `ClaudeQuickControls.tsx` / `PermissionModeSelector.tsx` / `AccountUsagePanel.tsx` / `TokenUsagePie.tsx` **是重新排列不是重寫**，它們的行為（模型過濾 effort、Haiku 沒有 adaptive thinking 等）已經調好了，不要在搬家過程中弄丟。

---

## 8. 配色改成 Claude Code 色系

**決策**：暖橘 ＋ 暖深灰，**深色淺色都換**。

**已驗證色值**：`#d97757` 確實出現在 Claude Code 擴充套件 CSS 裡（連同 `#c6613f` `#f5a66a` `#eda38a` 等同色階、`#faf9f5` 米白底）。
⚠️ 但要知道：擴充套件的**底色是繼承 VSCode 主題**（`var(--vscode-*)`），不是它自己定的。所以「跟 Claude Code 一樣」在底色上只能取神韻——暖中性灰，不是現在這種帶藍的深色。

**目標值（已跟使用者確認過）**：
| | 深色 | 淺色 |
|---|---|---|
| 底 | `#262624` | `#FAF9F5` |
| 卡片 | `#30302E` | — |
| 重點色 | `#D97757` | `#C6613F` |
| 文字 | `#F5F4EF` | `#262624` |

**好消息**：`F:\Vs\cloudcli\src\index.css:24` 起全走 CSS 變數（HSL 格式），改一組變數就全站換色。
**壞消息**：仍有寫死顏色沒走變數的地方，至少已知：
- `MessageComponent.tsx:122` 使用者訊息泡泡寫死 `bg-blue-600`
- `ChatMessagesPane.tsx:198,220,225` 寫死 `bg-blue-600` / `text-blue-600`
這類要一起掃掉，不然會出現一顆藍泡泡浮在暖色介面上。

**注意**：`src/index.css` 的變數是 **HSL 三元組格式**（如 `222.2 84% 4.9%`），不是 hex。上面的 hex 要換算過去，別直接貼。

⚠️ **風險**：全站改色**沒有自動化方法確認沒漏**，必須開圖一頁一頁看。這項一定是 ❌ 未測，等使用者回報哪裡還是藍的。

---

## 9. 圓角與密度

**決策修正**（使用者註記推翻了表單選項的重點）：
> 「也不是說工具感，美感還要有的，我覺得主要是**文字不夠貼框**，圓角其實應該還好。」

所以**主力是壓縮留白讓文字貼框**，圓角只收一半、保留一點圓，**不要拉成直角**。

**契約**：
- `F:\Vs\cloudcli\tailwind.config.js` 的 `borderRadius` 吃 `--radius`（目前 `0.5rem`）→ 收到約一半
- 全站另有寫死圓角 384 處（`rounded-lg` 229、`rounded-full` 107、`rounded-xl` 41、`rounded-2xl` 7）。**`rounded-full` 的 107 處大多是頭像和圓點，那些該保持圓的**，別無差別替換
- 留白壓縮主要對象：訊息氣泡的 `px-4 py-2`、卡片的 `p-3`/`p-4`、清單項的 `py-3` 這類

⚠️ **風險**：「貼框」是手感問題，Claude 沒辦法量化驗證。做一版請使用者看，按回饋調，預期要來回一兩次。

---

## 10. 捲動被拉走（兩邊都防）

**決策**：使用者不確定觸發情境，所以**兩個嫌疑都防**。

**兩個嫌疑（都在 `F:\Vs\cloudcli\src\components\chat\hooks\useChatSessionState.ts`）**：

**嫌疑 A — 載入舊訊息後的位置還原算歪**（`:367-374`）
往上捲到 `scrollTop < 100` 觸發載入更早訊息，載完用 `scrollTop = top + (newHeight - oldHeight)` 還原。問題是這個差值在 **Markdown / 程式碼區塊 / 圖片還沒渲染完**時算的，高度之後才變 → 位置就歪掉。這最符合使用者描述的「被拉到二三次對話的位置」。

**嫌疑 B — 新訊息進來搶捲動**（`:666-681`）
`if (!isUserScrolledUp) setTimeout(scrollToBottom, 50)`。`isUserScrolledUp` 由 scroll 事件更新，捲動當下若新訊息剛好到，狀態還沒更新完 → 被拉到底。

**契約**：
- A 的修法方向：還原位置要等內容真的穩定（量測時機要對），不是加大 timeout 硬湊
- B 的修法方向：使用者只要有主動捲動意圖就別搶，判定要即時而不是靠 50ms 後補
- **不要動** `searchScrollActiveRef` 那條路徑，那是從搜尋跳轉進來的，行為是對的

⚠️ **無法自行重現**：Claude 沒辦法在沒有真實長對話 + 真實手機捲動的情況下重現。修完只能標 ⚠️ 半驗，等使用者回報還會不會發生。

---

## 11. 右緣對話跳轉刻度

**決策**：右邊緣一條細刻度條，每則提問一段。
**使用者註記**：「**不要是點，可以是個長條**，比較好點擊」→ 是**長條分段**，不是圓點。

**契約**：
- 每則**使用者提問**對應一段長條（不是每則訊息）
- 平常半透明，碰到／滑到才變清楚
- **絕對不能擠壓正文行寬** — 使用者原話「不要去擠壓到行寬」。所以是浮在上層（absolute/fixed），不是佔一欄
- 點一下跳到對應那則提問
- 掛在 `F:\Vs\cloudcli\src\components\chat\view\subcomponents\ChatMessagesPane.tsx` 的捲動容器上

**邊界條件**：
- 對話很長（上百則）時，長條會被壓到極短 → 要有最小高度，超過就聚合
- 訊息還沒全部載入時（分頁只載了一部分），刻度只能反映**已載入**的範圍，不能假裝知道全部
- 手機觸控目標要夠大（這正是使用者要長條不要點的原因）

---

## 12. 上游更新：先看清單再決定要抄什麼

**現況**：我們 v1.32.0，上游 v1.37.1，**落差 161 個 commit**。使用者決定「不直接用它的更新」，改成手動挑功能抄。

### 值得考慮抄的（**每條都讀過實際 diff**，不是照 commit 標題猜）

| 上游功能 | commit | 查證結果 | 抄的成本 |
|---|---|---|---|
| **搜尋對不到 Claude 對話** | `9507694` | ✅ **真 bug**：transcript 每行用 `provider_session_id` 標記，程式卻拿 app 的 `session_id` 比對 → 對不上。我們有 `session-conversations-search.service.ts` 且從沒改過 | **12 行**，最划算 |
| **token 判定過期的時鐘偏移** | `f0dca2d` | ✅ 實際只有 **3 行**：加 `TOKEN_EXPIRY_SKEW_MS = 60_000`。原本瀏覽器時鐘快一點就把伺服器還認的 token 當過期丟掉。`src/utils/api.js` 我們沒改過 | 3 行 |
| **對話跑完出聲＋標題提示** | `d70dc07` | ✅ 兩個獨立新檔（`notificationSound.ts` / `pageTitleNotification.ts`）＋設定分頁，316 行，不碰我們改過的檔。⚠️ **它附的 zh-TW 翻譯要丟掉，用我們自己那份** | 中 |
| **側欄「最近對話」feed** | `015e892` | ⚠️ 動到 `server/modules/providers/provider.routes.ts` — **我們在那加了帳號切換 API**，必衝突。另需 DB repository 新增查詢 | 中高 |
| **手機終端機修復** | `b6cf333` | ⚠️ 1123 行大改：新增 1068 行觸控選字模組、**刪掉整個 `ShellMinimalView`**。要先確認使用者真的在手機上用終端機 | 高 |
| **Skills / MCP 設定改版** | `c5fe127` `41e0d30` | 想在手機上裝 skill 才需要 | 高 |

### 查證後撤回的（上一版清單寫錯，別再被它誤導）

| 原本以為 | 實際查到 |
|---|---|
| ~~上游修好了 WebSocket 半死連線~~ | ❌ **完全沒有**。上游 `WebSocketContext.tsx`（214 行）grep `ping`/`pong`/`heartbeat`/`visibilitychange` **一個都沒有**，跟我們一樣。誤會來自 `fix(shell): recover from stale websocket connections` — 那是**終端機的 PTY socket**，不是 chat 連線，而且包在 `06e7ee9` 那個 254 檔案／19000 行的巨型 commit 裡，挑不出來。**這個坑只能我們自己修**（修法見 memory `feedback-websocket-halfdead-connection`：應用層 ping ＋ `visibilitychange` 回前景檢查） |
| ~~檔案樹不掃系統目錄很有用~~ | ❌ 降級。它擋的是 `/proc` `/sys`，commit 明說是「`/proc` 上千虛擬項目讓遍歷病態地慢」——**Linux 特有**。`FORBIDDEN_WORKSPACE_PATHS` 確實有 Windows 段（`C:\Windows` 等）所以非零價值，但機率遠低於原本以為的 |
| ~~點 token 用量開成本明細~~ | ❌ **前提不成立**。它改 `TokenUsageSummary.tsx`，我們只有 `TokenUsagePie.tsx`（上游後來換掉元件）→ diff 貼不上，等於重寫 |

**上游 `WebSocketContext.tsx` 比我們多的 83 行是什麼**（跟半死連線無關，但有參考價值）：
`subscribe` listener registry（避免高頻串流被 React batching 合併掉／丟訊息）、token 過期與 auth loading 守衛、cleanup 時把 `onopen/onmessage/onclose/onerror` 全部清成 null（避免舊 socket 在換 token 後又排一次重連）。最後這點若我們自己做心跳重連，值得一起參考。

### 明確不抄

| 上游功能 | 為什麼不抄 |
|---|---|
| **設計改版 `7eb7348`** | 跟我們這次全站配色／圓角／密度大改**正面衝突**，抄了等於白做 |
| **Electron 桌面版 `97c9b67`** | 我們走 PWA 路線，已經定案 |
| **繁體中文語系 `c21a9f4`** | 上游後來自己加了 zh-TW，但我們已有自己那份、用詞也調過。**不要覆蓋** |
| **effort 控制 `d272922`** | 我們自己已經做了，而且是依模型動態過濾的版本 |
| **西班牙文／韓文／法文語系** | 用不到 |
| **Codex / plugin 相關** | 用不到 |

⚠️ **抄的時候的鐵則**：一次抄一個、抄完就 build 一次確認沒壞，**不要一口氣抄五個再一起測**。上游那些 commit 建立在它自己後面 160 個 commit 的基礎上，直接 cherry-pick 多半會失敗，多數要用「讀它的 diff、手動改我們的版本」。

---

## 執行順序建議

1. **先做第 2 項（拔 Upgrade）** — 危險品，而且最快，五分鐘的事
2. **再做 1、3、4、5** — 各自獨立、互不干擾，做完就有感
3. **然後 6、7** — 動到版面結構
4. **接著 8、9** — 全站配色與密度，改動面最大，放在結構定案之後才不用改兩次
5. **最後 10、11** — 捲動行為與新元件，要在版面穩定後才好調
6. **第 12 項另開一次工作**，不要混在這批裡

**每做完一項就 build 一次**，不要積五項再一起測——出事了會分不清是哪一項造成的。
